name: CI

# 日常检查：push / PR 触发，只做快速导入冒烟 + Rust 检查，不打包。
# 打包发布走 release.yml（tag 驱动）。
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  python-check:
    name: mdtool 导入冒烟
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: 安装 uv
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: 安装依赖（含本地 libmarkdown）
        run: |
          uv sync
          uv sync --extra ast

      - name: 导入冒烟测试（desktop + core 全部模块可导入）
        run: |
          uv run python -c "import mdtool.desktop.qtui.main_window, mdtool.desktop.qtui.widgets, mdtool.desktop.qtui.icons, mdtool.desktop.qtui.workers, mdtool.desktop.qtui.theme, mdtool.desktop.qtui.logbus; import mdtool.desktop.qtui.tabs.library, mdtool.desktop.qtui.tabs.search, mdtool.desktop.qtui.tabs.media_server, mdtool.desktop.qtui.tabs.space_fix, mdtool.desktop.qtui.tabs.image_check, mdtool.desktop.qtui.tabs.migrate, mdtool.desktop.qtui.tabs.file_browser, mdtool.desktop.qtui.tabs.notes_browser; import mdtool.core.server.notes_db, mdtool.core.server.meta_db, mdtool.core.server.media_server, mdtool.core.utils, mdtool.core.link_resolver; print('imports OK')"

      - name: 单元测试（link_resolver / zip 打包计划）
        run: |
          uv run python -m pytest -q

      - name: boot 冒烟（QApplication 下应用主题 + 构造 MainWindow + 图标渲染 + shutdown）
        env:
          QT_QPA_PLATFORM: offscreen
        run: |
          uv run python -c "import sys; from PySide6.QtWidgets import QApplication; app=QApplication(sys.argv); from mdtool.desktop.qtui.theme import apply_theme; apply_theme(app,'light'); from mdtool.desktop.qtui.main_window import MainWindow; w=MainWindow(); assert w.stack.count()==7; from mdtool.desktop.qtui.icons import folder_icon, file_icon, tray_icon, nav_icon; folder_icon(); file_icon(); tray_icon(); [nav_icon(n) for n in ('notes','search','server','check','migrate','space','log')]; [t.shutdown() for t in w.all_tabs()]; print('boot OK')"

  libmarkdown-test:
    name: libmarkdown 单元测试
    runs-on: windows-latest
    defaults:
      run:
        working-directory: libmarkdown
    steps:
      - uses: actions/checkout@v4
      - name: 安装 uv
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - name: 安装依赖
        run: uv sync
      - name: 运行测试
        run: uv run pytest -q

  rust-check:
    name: typora-uploader cargo check
    runs-on: windows-latest
    defaults:
      run:
        working-directory: typora-uploader
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: typora-uploader
      - name: cargo check
        run: cargo check --release
